# React + LM Studio Demo

This project demonstrates a React frontend connected to a locally running LM Studio model.

## Requirements
- Node.js
- LM Studio (local server running)

## Setup
npm install
npm start

## Notes
LM Studio runs locally; models are not included in this repo.

